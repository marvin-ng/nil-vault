kind = "web"
previewPath = "/"
title = "NIL Vault"
version = "1.0.0"
id = "artifacts/nil-vault"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19482

[services.development]
run = "pnpm --filter @workspace/nil-vault run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nil-vault", "run", "build" ]
publicDir = "artifacts/nil-vault/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19482"
BASE_PATH = "/"
